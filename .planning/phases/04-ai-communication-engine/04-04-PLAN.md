---
phase: 04-ai-communication-engine
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/backend/src/types/entities.ts
  - packages/frontend/src/lib/hooks/use-conversations.ts
  - packages/frontend/src/components/features/inbox/draft-card.tsx
  - packages/frontend/src/lib/format.ts
autonomous: true
requirements: [AI-03]
gap_closure: true

must_haves:
  truths:
    - "AI draft card in inbox shows cost in EUR alongside token breakdown"
    - "AI draft card shows input tokens, output tokens, and cache metrics"
    - "Edge case flags (complaint, medical, dietary, cancellation, adoption) display as warning badges on the draft card"
  artifacts:
    - path: "packages/backend/src/types/entities.ts"
      provides: "AiDraft entity type with all token tracking fields"
      contains: "inputTokens"
    - path: "packages/frontend/src/lib/hooks/use-conversations.ts"
      provides: "Frontend AiDraft interface with all token tracking fields"
      contains: "costEur"
    - path: "packages/frontend/src/components/features/inbox/draft-card.tsx"
      provides: "Draft card rendering cost, tokens, and flags"
      contains: "formatCostMicrocents"
    - path: "packages/frontend/src/lib/format.ts"
      provides: "Microcent cost formatter for AI costs"
      contains: "formatCostMicrocents"
  key_links:
    - from: "packages/frontend/src/components/features/inbox/draft-card.tsx"
      to: "packages/frontend/src/lib/hooks/use-conversations.ts"
      via: "import AiDraft type"
      pattern: "import.*AiDraft.*use-conversations"
    - from: "packages/frontend/src/components/features/inbox/draft-card.tsx"
      to: "packages/frontend/src/lib/format.ts"
      via: "import formatCostMicrocents"
      pattern: "import.*formatCostMicrocents.*format"
---

<objective>
Surface AI token usage and EUR cost data in the admin inbox UI, closing the AI-03 verification gap.

Purpose: The backend stores granular token tracking (inputTokens, outputTokens, cacheReadTokens, cacheWriteTokens, costEur, provider, durationMs, flags) on every AiDraft record, but the frontend types and UI only show the legacy `tokensUsed` field. This makes AI cost data invisible to Ines in the admin dashboard, failing the "viewable in admin" requirement of AI-03.

Output: Updated type definitions (backend entities.ts + frontend use-conversations.ts), new cost formatter, and enhanced draft-card.tsx that displays EUR cost, token breakdown, and edge-case flag badges.
</objective>

<execution_context>
@/Users/iavi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iavi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ai-communication-engine/04-VERIFICATION.md
@packages/backend/src/types/entities.ts
@packages/frontend/src/lib/hooks/use-conversations.ts
@packages/frontend/src/components/features/inbox/draft-card.tsx
@packages/frontend/src/lib/format.ts
@packages/backend/src/services/ai/cost-calculator.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update AiDraft types in backend and frontend</name>
  <files>
    packages/backend/src/types/entities.ts
    packages/frontend/src/lib/hooks/use-conversations.ts
    packages/frontend/src/lib/format.ts
  </files>
  <action>
1. In `packages/backend/src/types/entities.ts`, update the `AiDraft` type (lines 240-250) to include all fields from the Prisma schema:
   - Keep existing fields: id, conversationId, messageId, content, status, model, tokensUsed, createdAt, updatedAt
   - Add: `inputTokens: number` (default 0)
   - Add: `outputTokens: number` (default 0)
   - Add: `cacheReadTokens: number` (default 0)
   - Add: `cacheWriteTokens: number` (default 0)
   - Add: `costEur: number` (microcents, EUR * 100,000)
   - Add: `provider: string` (e.g., "anthropic")
   - Add: `durationMs: number` (default 0)
   - Add: `flags: string[]` (edge case flags, default [])

2. In `packages/frontend/src/lib/hooks/use-conversations.ts`, update the `AiDraft` interface (lines 29-38) to match:
   - Keep existing fields: id, messageId, conversationId, content, status, model, tokensUsed, createdAt
   - Add same fields as above: inputTokens, outputTokens, cacheReadTokens, cacheWriteTokens, costEur, provider, durationMs, flags

3. In `packages/frontend/src/lib/format.ts`, add a `formatCostMicrocents` function:
   - Takes `microcents: number` (EUR * 100,000)
   - Returns a string like "0.0134" (4 decimal places) prefixed with euro sign
   - Implementation: `(microcents / 100_000).toFixed(4)` with euro sign prefix
   - This mirrors the backend `formatCostEur` from `cost-calculator.ts` but adds the euro sign for display
   - Export the function alongside existing formatCurrency, formatDate, etc.
  </action>
  <verify>
Run `cd /Users/iavi/Projects/PYR/packages/backend && npx tsc --noEmit` to confirm backend types compile.
Run `cd /Users/iavi/Projects/PYR/packages/frontend && npx tsc --noEmit` to confirm frontend types compile.
  </verify>
  <done>
AiDraft type in entities.ts includes all 8 new fields. AiDraft interface in use-conversations.ts matches. formatCostMicrocents exists in format.ts. Both packages compile cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update draft-card.tsx to display cost, tokens, and flags</name>
  <files>
    packages/frontend/src/components/features/inbox/draft-card.tsx
  </files>
  <action>
Update the DraftCard component in `draft-card.tsx` to display the new AiDraft fields:

1. **Import** `formatCostMicrocents` from `@/lib/format`.

2. **Replace the token badge section** (currently shows `{draft.tokensUsed} tokens`). In the header area where model and token badges are, render:
   - Model badge (keep existing): `{draft.model}` in an outline badge
   - Provider badge: `{draft.provider}` in an outline badge (only if provider exists and differs from model, e.g., "anthropic")
   - Cost badge: `{formatCostMicrocents(draft.costEur)}` in a secondary badge -- only show if `draft.costEur > 0`
   - Token breakdown: `{draft.inputTokens}in / {draft.outputTokens}out` in a secondary badge -- only show if `draft.inputTokens > 0` (indicates new-style tracking). Fall back to showing `{draft.tokensUsed} tokens` if inputTokens is 0 (backward compat with old drafts)
   - Duration: if `draft.durationMs > 0`, show `{(draft.durationMs / 1000).toFixed(1)}s` as a small muted text
   - Cache info: if `draft.cacheReadTokens > 0`, show a small "cached" indicator (e.g., a Badge variant="outline" with text "cache hit")

3. **Add edge-case flag badges** below the header badges row. For each flag in `draft.flags`, render a warning-styled Badge:
   - Use `variant="destructive"` for "complaint" and "cancellation"
   - Use `variant="outline"` with amber/yellow text color for "medical", "dietary", "adoption"
   - Capitalize the flag name for display (e.g., "Complaint", "Medical")
   - Add `AlertTriangle` icon import from lucide-react for flags row
   - Show a text hint "Sensitive -- review carefully" in muted text when any flags are present

4. **Layout**: Keep the header row compact. The badges should wrap naturally if there are many. The flags row goes below the existing header if flags exist. Use `flex-wrap gap-1.5` for badge containers.

Do NOT change any approval/reject/edit logic -- only the display portions.
  </action>
  <verify>
Run `cd /Users/iavi/Projects/PYR/packages/frontend && npx tsc --noEmit` to confirm no type errors.
Run `cd /Users/iavi/Projects/PYR/packages/frontend && pnpm build` to confirm the full build passes.
  </verify>
  <done>
Draft card displays: EUR cost badge, input/output token breakdown, cache hit indicator, duration, and edge-case flag badges with appropriate severity coloring. Frontend builds cleanly. Old drafts (inputTokens=0) fall back gracefully to showing legacy tokensUsed.
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/iavi/Projects/PYR/packages/backend && npx tsc --noEmit` -- no type errors
2. `cd /Users/iavi/Projects/PYR/packages/frontend && pnpm build` -- builds without errors
3. Grep `draft-card.tsx` for `costEur` -- confirms cost rendering is present
4. Grep `draft-card.tsx` for `flags` -- confirms edge case flag rendering is present
5. Grep `entities.ts` for `inputTokens` -- confirms backend type updated
6. Grep `use-conversations.ts` for `costEur` -- confirms frontend type updated
</verification>

<success_criteria>
- AI-03 gap closed: token usage (input/output) and EUR cost are visible on draft cards in the admin inbox
- Edge-case flags display as colored badges on draft cards
- Backward compatibility: old drafts with only tokensUsed still render correctly
- Both backend and frontend compile and build cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/04-ai-communication-engine/04-04-SUMMARY.md`
</output>
