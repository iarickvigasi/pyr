---
phase: 05-ai-email-integration
plan: 04
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - packages/backend/src/services/ai/__tests__/draft-pipeline.test.ts
  - packages/backend/src/modules/inbox/__tests__/draft-workflow.test.ts
autonomous: true
requirements: [TEST-02]

must_haves:
  truths:
    - "Integration tests verify the full AI draft pipeline: email arrival triggers draft generation with correct context injection"
    - "Tests verify per-message dedup: two messages in the same conversation each generate separate drafts"
    - "Tests verify onFailed handler writes a failed draft record to the database"
    - "Tests verify the approve workflow: draft approval sends email via SMTP and stores outbound message"
    - "Tests verify reject and regenerate workflows update draft status correctly"
    - "Tests verify FAQ entries are included in the system prompt during draft generation"
    - "All tests use mocked LLM responses (no real API calls)"
  artifacts:
    - path: "packages/backend/src/services/ai/__tests__/draft-pipeline.test.ts"
      provides: "End-to-end draft pipeline tests with mocked OpenClaw"
      contains: "describe.*draft pipeline"
    - path: "packages/backend/src/modules/inbox/__tests__/draft-workflow.test.ts"
      provides: "Draft approve/reject/regenerate endpoint tests"
      contains: "describe.*draft workflow"
  key_links:
    - from: "packages/backend/src/services/ai/__tests__/draft-pipeline.test.ts"
      to: "packages/backend/src/services/ai/draft-generator.ts"
      via: "tests call generateDraft with mock Prisma and stub fetch"
      pattern: "generateDraft"
    - from: "packages/backend/src/modules/inbox/__tests__/draft-workflow.test.ts"
      to: "packages/backend/src/modules/inbox/conversation.service.ts"
      via: "tests call approveDraft, rejectDraft, regenerateDraft with mock Prisma"
      pattern: "approveDraft|rejectDraft|regenerateDraft"
---

<objective>
Write integration tests verifying the full AI draft pipeline (context injection, LLM call, draft storage) and draft workflow endpoints (approve, reject, regenerate) with mocked LLM responses.

Purpose: Prove the AI draft generation pipeline works end-to-end and the draft approval workflow is correct -- all with mocked LLM responses so tests run fast and reliably (TEST-02).
Output: Two test files covering pipeline integration and workflow correctness.
</objective>

<execution_context>
@/Users/iavi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iavi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ai-email-integration/05-CONTEXT.md
@.planning/phases/05-ai-email-integration/05-RESEARCH.md
@.planning/phases/05-ai-email-integration/05-01-SUMMARY.md
@.planning/phases/05-ai-email-integration/05-02-SUMMARY.md
@packages/backend/src/services/ai/__tests__/draft-generator.test.ts
@packages/backend/src/services/ai/draft-generator.ts
@packages/backend/src/services/ai/context-builder.ts
@packages/backend/src/modules/inbox/conversation.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write draft pipeline integration tests (context injection, LLM call, draft storage, FAQ inclusion)</name>
  <files>packages/backend/src/services/ai/__tests__/draft-pipeline.test.ts</files>
  <action>
Create a new test file following the existing pattern from `draft-generator.test.ts`. Use vitest with mocked fetch and mock Prisma.

Test cases:

1. **"generates draft for guest inquiry email with full context"**
   - Set up mock Prisma with: conversation (guest_inquiry), guest record, messages array, bookings, room types, events
   - Mock fetch to return a valid OpenClaw chat completions response
   - Call generateDraft with conversationId, messageId, guestLanguage: 'en'
   - Assert: draft is stored (prisma.aiDraft.create called), result has draftId, content, model, costEur >= 0
   - Assert: system prompt sent to OpenClaw includes guest name, conversation context, availability data

2. **"includes FAQ entries in system prompt"**
   - Same setup but add FAQ entries to mock Prisma: `prisma.faq.findMany` returns `[{ question: 'What time is check-in?', answer: 'Check-in is at 3 PM.' }]`
   - Call generateDraft
   - Assert: the system prompt in the fetch call body includes "Frequently Asked Questions" and "What time is check-in?"

3. **"handles empty FAQ list gracefully"**
   - Set up with `prisma.faq.findMany` returning `[]`
   - Call generateDraft
   - Assert: system prompt includes FAQ section with "No FAQ entries available" fallback text
   - Assert: draft still generates successfully

4. **"classifies edge cases and stores flags"**
   - Set up a conversation where the last message contains "I need to cancel my booking"
   - Call generateDraft
   - Assert: result.flags includes 'cancellation'
   - Assert: stored draft has flags array with 'cancellation'

5. **"handles OpenClaw API failure gracefully"**
   - Mock fetch to return `{ ok: false, status: 500 }`
   - Call generateDraft
   - Assert: throws an error (which BullMQ will retry)

6. **"two messages in the same conversation each generate separate drafts"**
   - Set up mock Prisma with: conversation with two inbound messages (msg1, msg2)
   - First call: generateDraft with messageId=msg1.id → succeeds, creates draft for msg1
   - Set up: `prisma.aiDraft.findFirst` returns existing draft for msg1 when queried with msg1 messageId, returns null when queried with msg2 messageId
   - Second call: simulate the job processor dedup check with messageId=msg2.id
   - Assert: dedup query uses `{ conversationId, messageId: msg2.id, status: { in: ['pending', 'failed'] } }` — NOT just `{ conversationId, status: 'pending' }`
   - Assert: second draft is NOT skipped (since there's no draft for msg2)

7. **"onFailed handler creates a failed draft record in the database"**
   - Call `createAiDraftFailedHandler(app)` with a mock job containing conversationId and messageId, and a mock Error
   - Assert: `prisma.aiDraft.create` called with `{ status: 'failed', conversationId, messageId, content: '', model: 'none', tokensUsed: 0 }`
   - Assert: no error thrown even if the DB write itself fails (logs error instead)

8. **"generates draft in German when guest language is 'de'"**
   - Set guestLanguage: 'de'
   - Assert: system prompt includes language instruction for German response

Follow existing patterns from draft-generator.test.ts:
- Use `vi.stubGlobal('fetch', vi.fn())` for OpenClaw mock
- Create helper functions: `makeOpenClawResponse()`, `makeMockPrisma()`, `makeConfig()`
- Mock Prisma with Proxy pattern for model access (same as existing test)
- Use `vi.mock` for audit log to prevent side effects
  </action>
  <verify>Run `pnpm test -- --run src/services/ai/__tests__/draft-pipeline.test.ts` from packages/backend. All tests should pass. Then run full `pnpm test` to ensure no regressions.</verify>
  <done>8 integration tests verify the draft pipeline end-to-end: context assembly, FAQ injection, edge-case classification, error handling, per-message dedup, onFailed handler, and language selection -- all with mocked LLM.</done>
</task>

<task type="auto">
  <name>Task 2: Write draft workflow tests (approve sends SMTP, reject updates status, regenerate enqueues job)</name>
  <files>packages/backend/src/modules/inbox/__tests__/draft-workflow.test.ts</files>
  <action>
Create a new test file for the draft approve/reject/regenerate service functions. Follow vitest patterns from existing test files.

Test cases for `approveDraft`:

1. **"approves draft and sends email via SMTP"**
   - Set up mock Prisma with: pending AiDraft, conversation with guest email, messages with messageIds for threading
   - Mock email module (dynamic import): vi.mock for createEmailModule returning sendEmail that resolves with messageId
   - Mock buildReferencesChain (dynamic import)
   - Call approveDraft(prisma, app, conversationId, draftId)
   - Assert: draft status updated to 'approved'
   - Assert: outbound message created (direction: 'out', channel: 'email')
   - Assert: sendEmail called with correct to address and threading headers
   - Assert: audit log written with trigger: 'ai-draft-approve'

2. **"approves edited draft with modified content"**
   - Call approveDraft with editedContent="Modified reply"
   - Assert: draft status updated to 'edited'
   - Assert: draft content updated to "Modified reply"
   - Assert: sendEmail called with the edited content

3. **"rejects approve for non-pending draft"**
   - Set up draft with status 'approved'
   - Call approveDraft
   - Assert: throws BadRequestError

4. **"rejects approve for non-existent draft"**
   - Set up prisma.aiDraft.findUnique returning null
   - Assert: throws NotFoundError

Test cases for `rejectDraft`:

5. **"rejects pending draft"**
   - Call rejectDraft(prisma, conversationId, draftId)
   - Assert: draft status updated to 'rejected'
   - Assert: audit log written

6. **"throws for non-pending draft rejection"**
   - Draft status 'approved'
   - Assert: throws BadRequestError

Test cases for `regenerateDraft`:

7. **"rejects old draft and enqueues new AI draft job"**
   - Set up mock BullMQ queue (app.queues.getQueue returns mock with add method)
   - Call regenerateDraft(prisma, app, conversationId, draftId)
   - Assert: old draft status updated to 'rejected'
   - Assert: BullMQ add called with 'ai-draft' job and correct data
   - Assert: audit log written with action: 'regenerate'

8. **"reads guest language from profile for regeneration"**
   - Guest with language: 'de'
   - Call regenerateDraft
   - Assert: BullMQ job data includes guestLanguage: 'de'

Mock patterns:
- Mock Prisma: create mock with `$transaction` that passes through to callback with the mock itself
- Mock email module: use vi.mock with factory for dynamic imports of createEmailModule and buildReferencesChain
- Mock BullMQ queue: `app.queues = { getQueue: vi.fn().mockReturnValue({ add: vi.fn() }) }`
- Mock audit log: vi.mock('../../lib/audit.js')
- Import service functions directly for unit testing
  </action>
  <verify>Run `pnpm test -- --run src/modules/inbox/__tests__/draft-workflow.test.ts` from packages/backend. All tests should pass. Then run full `pnpm test` to ensure no regressions.</verify>
  <done>8 workflow tests verify: approve sends email + stores message, edited approve uses modified content, reject updates status, regenerate enqueues new job, error cases throw correct exceptions.</done>
</task>

</tasks>

<verification>
1. `pnpm test` passes in packages/backend with all new tests green
2. Draft pipeline tests cover: context injection, FAQ inclusion, edge-case flags, error handling, per-message dedup, onFailed handler, language selection
3. Workflow tests cover: approve (original + edited), reject, regenerate, error cases (not found, wrong status)
4. No real API calls made during tests (all fetch mocked)
5. No real database required (all Prisma mocked)
6. Total new tests: ~16 across 2 files
</verification>

<success_criteria>
- Draft pipeline integration tests verify context injection -> LLM call -> draft stored (TEST-02)
- FAQ entries verified in system prompt during draft generation (TEST-02)
- Approve/reject/regenerate workflow tests verify correct state transitions (TEST-02)
- All tests use mocked LLM and mocked Prisma -- no external dependencies (TEST-02)
- Existing tests remain green
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-email-integration/05-04-SUMMARY.md`
</output>
