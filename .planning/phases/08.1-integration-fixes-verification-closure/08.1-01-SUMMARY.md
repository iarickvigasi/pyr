---
phase: 08.1-integration-fixes-verification-closure
plan: 01
subsystem: api, assistant
tags: [bullmq, caldav, calendar-sync, openclaw, availability, integration-test, traceability]

# Dependency graph
requires:
  - phase: 03-email-ui-ota-parsing
    provides: OTA booking creation in email pipeline
  - phase: 06-caldav-calendar-sync
    provides: Calendar sync queue and CalendarSyncJobData type
  - phase: 07-openclaw-assistant-core
    provides: OpenClaw plugin with check_availability tool
provides:
  - OTA-created bookings trigger CalDAV calendar sync via BullMQ
  - check_availability tool sends correct checkIn/checkOut params to backend
  - Comprehensive traceability table with all v1 + v2 requirements
affects: [09-testing-migration-launch]

# Tech tracking
tech-stack:
  added: [vitest (openclaw-plugin devDep)]
  patterns: [calendar sync enqueue after OTA booking, shared mock IMAP pattern for email pipeline tests]

key-files:
  created:
    - packages/backend/src/services/email/__tests__/ota-calendar-sync.test.ts
    - packages/assistant/openclaw-plugin/__tests__/check-availability.test.ts
  modified:
    - packages/backend/src/services/email/index.ts
    - packages/assistant/openclaw-plugin/tools/rooms.ts
    - packages/assistant/openclaw-plugin/package.json
    - .planning/REQUIREMENTS.md

key-decisions:
  - "Shared mock IMAP instance (module-level vi.fn) for email pipeline tests instead of per-test factory -- ensures createImapService mock returns consistent pollNewEmails spy"
  - "vitest added as devDep to openclaw-plugin to enable standalone test execution"

patterns-established:
  - "OTA booking calendar sync: calQueue.add after transaction commit with inner try/catch for error isolation"

requirements-completed: [OTA-03, CAL-01, ASST-04]

# Metrics
duration: 5min
completed: 2026-02-22
---

# Phase 8.1 Plan 01: Integration Fixes & Traceability Summary

**OTA booking calendar sync wiring + check_availability param fix with 10 integration tests and comprehensive traceability table**

## Performance

- **Duration:** 5 min
- **Started:** 2026-02-22T19:02:28Z
- **Completed:** 2026-02-22T19:07:48Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- OTA-created bookings now trigger CalDAV calendar sync via BullMQ (same pattern as booking.routes.ts)
- check_availability assistant tool sends checkIn/checkOut params matching backend availabilityQuerySchema
- 4 integration tests prove OTA calendar sync wiring (enqueue, ordering, error resilience, missing queue)
- 6 integration tests prove check_availability uses correct params (param names, API call, response format)
- REQUIREMENTS.md traceability table expanded with all 14 v2 requirements as Deferred (v2), totaling 61 tracked requirements

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix OTA booking calendar sync wiring and check_availability params** - `5be7b89` (fix)
2. **Task 2: Integration tests for bug fixes and traceability table update** - `94ae35c` (test)

## Files Created/Modified
- `packages/backend/src/services/email/index.ts` - Added CalendarSyncJobData import and calendar sync enqueue after OTA booking creation
- `packages/assistant/openclaw-plugin/tools/rooms.ts` - Changed from/to params to checkIn/checkOut matching backend schema
- `packages/backend/src/services/email/__tests__/ota-calendar-sync.test.ts` - 4 tests: enqueue, ordering, error resilience, missing queue
- `packages/assistant/openclaw-plugin/__tests__/check-availability.test.ts` - 6 tests: param registration, API call verification, response format
- `packages/assistant/openclaw-plugin/package.json` - Added vitest devDependency and test scripts
- `.planning/REQUIREMENTS.md` - Added v2 requirements to traceability table, updated coverage counts

## Decisions Made
- Used module-level `vi.fn()` for shared mock IMAP pollNewEmails instead of per-test factory. The `createImapService` mock factory needs to return the same `pollNewEmails` spy that beforeEach configures, which requires declaring it at module scope.
- Added vitest as a devDependency to the openclaw-plugin package rather than running tests through the backend's vitest. This keeps test execution self-contained per package.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added vitest devDependency to openclaw-plugin**
- **Found during:** Task 2 (check-availability test creation)
- **Issue:** The openclaw-plugin package had no test runner -- vitest was only in the backend package
- **Fix:** Added vitest ^3.0.4 to devDependencies and test/test:watch scripts to package.json
- **Files modified:** packages/assistant/openclaw-plugin/package.json, pnpm-lock.yaml
- **Verification:** `pnpm --filter @pyr/openclaw-plugin exec vitest run` executes successfully
- **Committed in:** 94ae35c (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Necessary for test execution. No scope creep.

## Issues Encountered
- Initial IMAP mock approach (calling `vi.mocked(createImapService)()` in beforeEach) created a different mock instance than what `createEmailModule` used internally. Fixed by declaring `mockPollNewEmails` at module scope and referencing it in the `vi.mock` factory.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Both cross-phase wiring bugs are fixed and tested
- Traceability table is comprehensive (all v1 + v2 requirements)
- Ready for Plan 02: retroactive VERIFICATION.md files for Phases 6 and 7

---
*Phase: 08.1-integration-fixes-verification-closure*
*Completed: 2026-02-22*
