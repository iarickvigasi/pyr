// AI context builder — implemented in E6 (AI Communication Engine).
// Assembles the system prompt for each guest message: injects CRM data (guest profile,
// booking history, language preference), room availability, and FAQ content.
// Business rule: always respond in the guest's language (EN or DE) and sign off as Ines.
export {};
