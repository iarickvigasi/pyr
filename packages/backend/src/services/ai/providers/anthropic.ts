// Anthropic (Claude) API adapter — implemented in E6.
// Primary LLM provider. Wraps the Anthropic SDK with the model-agnostic interface
// expected by ai-engine.ts. Default model: claude-sonnet-4-5-20250929.
// Logs token usage (input + output) and estimated cost on every call.
export {};
